#!/usr/bin/env groovy

node {
	
	def currentStep = CUR ? CUR : "NO CUR" 
	println currentStep
	
	def exceptionMsg = params.EXC ? params.EXC : "NO EXC"
	println exceptionMsg
	
	def branchName = params.BRA ? params.BRA : "NO BRA"
	println branchName

}